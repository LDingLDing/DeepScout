{
  "name": "deepscout",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.1.3"
  }
}
